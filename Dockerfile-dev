# Use uma imagem JDK completa para desenvolvimento (não só JRE)
FROM eclipse-temurin:21-jdk-alpine

LABEL authors="eduardo"

WORKDIR /app

# Copie apenas os arquivos de configuração do Maven inicialmente para cache de dependências
COPY .mvn/ .mvn
COPY mvnw pom.xml ./

RUN ./mvnw dependency:go-offline

# Copie o código fonte para permitir hot reload com volumes no docker-compose
COPY src ./src

# Exponha a porta da aplicação
EXPOSE 8080

# Variável de ambiente para ativar perfil dev no Spring
ENV SPRING_PROFILES_ACTIVE=dev

# Use mvn spring-boot:run para permitir hot reload de código durante o desenvolvimento
CMD ["./mvnw", "spring-boot:run"]